require('dotenv').config(); const { sendMail } = require('./src/config/mail'); async function test() { console.log('Testing Brevo API...'); console.log('API KEY:', process.env.BREVO_API_KEY ? process.env.BREVO_API_KEY.substring(0, 5) + '...' : 'Missing'); console.log('FROM:', process.env.MAIL_FROM); const res = await sendMail({ to: process.env.MAIL_FROM, subject: 'Test Email', html: '<p>Test.</p>', text: 'Test.' }); console.log('Result:', res); } test();
